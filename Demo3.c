#include<stdio.h> //standard input output header file or preprocessor directives

int main()
{
    int num,num2,sum=0;
    printf("enter the 2 number :");
    scanf("%d %d",&num,&num2);
    sum=num+num2;
    printf("the number1 is %d and number2 is %d and sum is %d ",num,num2,sum);

    return 0;
}

