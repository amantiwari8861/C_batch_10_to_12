#include<stdio.h>
#include<math.h>

int main()
{
    // int num,power=5,result=0;
    float result;
    float num2;
    printf("enter the number :");
    scanf("%f",&num2);

    // result=pow(num,power);
    // result=floor(num2); //downward rounding
    // result=ceil(num2);
    // result=sqrt(num2);
    printf("the result is %0.2f \n",result);
    return 0;
}