/*
functions has 2 types 
    1.pre-defined functions 
    2.user-defined functions
*/
#include<stdio.h>
#include<math.h>
#include "abc.h"

int main()
{
    // float power=2,num=6,result=0;
    //  result=pow(num,power);
    // result=log2(5);
    // result=log10(5);
    // printf("the power is %0.2f \n",result);
    printf("the value of %d\n",a);
    printf("the value of %d",fun());
    
    return 0;
}