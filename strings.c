#include<stdio.h>
#include<string.h>
#include<conio.h>

int main()
{
    /*char name[10],name2[20];
    gets(name);
    fflush(stdin);
    gets(name2);
    fflush(stdin);
    printf("%d",strcmp(name,name2));
    */
    char ch;

    
    ch=getch();
    printf("*\n");

    printf(" hello %c",ch);




    return 0;
}