#include<stdio.h>
int main()
{
    // a+b;
    //here a and b are the operands and + is the operator
    //1.unary operator
    //2.binary operator
        //2.1.arithmetic operator
        //2.2.relational operator
        //2.3.logical operator
        //2.4.bitwise operator
        //2.5.assignment operator
    //3.ternary operator(conditional operator)

    //1.unary operator eg. ++ (increment),-- (decrement)

    // int a=10,b=20;
    // printf("a=%d  b=%d before operation\n",a,b);
    // a++; //a=a+1 //a=11
    // a++; //a=a+1 //a=12
    // b--; //b=b-1 //b=19
    // printf("a=%d  b=%d after operation\n",a,b);

    //2.binary operator eg. +, -, *, /, %, <<, >>, &, |, &&, ||, !
    //2.1.arithmetic operator eg. +, -, *, /, %(modulus)
    // int c=10,d=20,e;
    // e=c+d; //e=c+d //e=30
    // printf("e=%d \n",c,d);
    // int a,b,c;
    // printf("enter two numbers\n");
    // scanf("%d %d",&a,&b);
    // c=a%b;
    // printf(" the remainder is %d\n",c);

    //2.2.relational operator eg. >, <, >=, <=, ==, !=
    // int a,b,c;
    // printf("enter two numbers\n");
    // scanf("%d %d",&a,&b);
    // c=(a>=b);
    // c=a==b;
    // c=a!=b;
    // printf(" a is greater than b ? : %d\n",c);
    // printf(" a is not equal to b ? : %d\n",c);

    //2.3.logical operator eg. &&, ||, !
    // int a,b,c;
    // printf("enter two numbers\n");
    // scanf("%d %d",&a,&b);




    return 0;
}
