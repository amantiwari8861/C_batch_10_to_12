#include<stdio.h>
int main()
{
    // int num=20,num2=10,result=0;
    printf("enter the number\n");
    // result=  ( num < num2 ) ? num : num2 ; //ternary operator
    // printf("the result is %d \n",result);
    int num=5;

    // num=num+10; //15
    num += 10;  //15 //shorthand operator or assignment operator
    printf("the num is %d \n",num);

    return 0;
}

