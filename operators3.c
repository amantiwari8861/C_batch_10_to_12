#include<stdio.h>

int main()
{
    //operator eg a+b here + is operator
    //operands eg a+b here a and b is operands

    //unary operators eg. ++,--
    /* int a=10;
    int result=++a; //increment -> a=a+1;
    printf(" value after increment is %d \n",result);//11
    printf(" value after increment is %d \n",a);//11
     */
    // int b=20;
    // b=b+1;  or ++b is same
    // b=b-1;  or --b is same
    // --b;
    // printf(" b value after decrement is %d ",b);
    
    //binary operators
        //arithematic operators
    /* int a=7,b=2,c;
    c=a+b;
    printf("the result is %d \n",c);
    c=a-b;
    printf("the result is %d \n",c);
    c=a*b;
    printf("the mul is %d \n",c);
    c=a/b;
    printf("the divide is %d \n",c);
    c=a%b;//modulus for remainder
    printf("the result is %d \n",c);
 */
        //relational operators
    int num1,num2,result;
    printf("enter 2 values : \n");
    scanf("%d%d",&num1,&num2);//  50 10
    //1 for true and 0 for false
    // result=num1<num2;
    // printf("the result is %d \n",result);
    // result=num1>num2;
    // printf("the result is %d \n",result);
    // result=num1>=num2; //50 >=50  -> 1
    // printf("the result is %d \n",result);
    // result=num1<=num2; //50 <=50  -> 1 or 30 <=50
    // printf("the result is %d \n",result);
    // result=num1==num2; //50 ==50  -> 1 or 50 == 20 -> 0
    // printf("the result is %d \n",result);
    // result=num1!=num2; //50 !=50  -> 0  or 50 != 20 -> 1
    // ! -> not = -> equal to ==>  !=  not equalto
    // printf("the result is %d \n",result);

    //logical oprerator


    

    return 0;
}
